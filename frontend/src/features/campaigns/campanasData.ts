export interface CampanasPresentationData {
  title: string;
  subtitle: string;
  notice: string;
  newButtonLabel: string;
}

export const campanasData: CampanasPresentationData = {
  title: "Campañas activas",
  subtitle:
    "Seguimiento de punta a punta: cada campaña se sigue desde la visita hasta la retención — campaña → usuario → interacción → conversión a plan de pago → activo a 90 días — con el mismo criterio que el funnel general.",
  notice:
    "Modo demo · los cambios se guardan sólo en este navegador.",
  newButtonLabel: "Nueva campaña",
};